package main

import "fmt"

// All code must belong to a package!
// We need a main function like in Java and C++
// This is always the main function and there can only be one in each package
// Only one entrypoint allowed in each package

// Print is a function that belongs to a built-in Go package, which is called fmt
func main() {
	fmt.Print("Hello World")
}
